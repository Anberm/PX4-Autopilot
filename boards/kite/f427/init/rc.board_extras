#!/bin/sh
#
# Kite F427 specific board extras init
#------------------------------------------------------------------------------

# Optional: Start FrSky Telemetry if configured
if param compare TEL_FRSKY_CONFIG 0
then
	frsky_telemetry start -d /dev/ttyS6 -t 15
fi
